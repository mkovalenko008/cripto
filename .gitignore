# секреты и окружение
.env
venv/
__pycache__/
*.pyc

# состояние и логи РЕАЛЬНОГО бота (main.py) — не нужны в публичном репо
state.json
trades_log.jsonl
bot.log

# лог paper-трейдеров — шумный, полезен только локально; в репо остаются
# *_state.json, *_trades_log.jsonl и *_STATUS.md
paper_bot.log
trend_paper_bot.log

# ручной kill switch — не должен случайно закоммититься включённым
STOP

# скачанные исторические данные для бэктеста — тяжёлые, воспроизводимы через fetch_history.py
data/
